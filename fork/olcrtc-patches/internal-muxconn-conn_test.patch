--- a/internal/muxconn/conn_test.go
+++ b/internal/muxconn/conn_test.go
@@ -41,6 +41,7 @@
 	s.sent = append(s.sent, append([]byte(nil), data...))
 	return s.sendErr
 }
+
 func (s *stubLink) CanSend() bool {
 	if s.canSendFn != nil {
 		return s.canSendFn()
@@ -49,6 +50,7 @@
 	defer s.mu.Unlock()
 	return s.canSend
 }
+
 func (s *stubLink) SendTo(peerID string, data []byte) error {
 	s.mu.Lock()
 	defer s.mu.Unlock()
@@ -81,6 +83,7 @@
 	}
 	return nil
 }
+
 func (l *loopLink) ControlSend(data []byte) error {
 	if l.peer.controlOnData != nil {
 		l.peer.controlOnData(bytes.Clone(data))
